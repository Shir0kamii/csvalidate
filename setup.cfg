[bumpversion]
current_version = 0.2.0
commit = True
tag = True
tag_name = {new_version}

[tool:pytest]
testpaths = tests
python_files = *.py
addopts = -vv --capture=no --showlocals --cov-report term-missing --cov csvalidate --no-cov-on-fail

[bumpversion:file:setup.py]

